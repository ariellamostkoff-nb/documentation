---
title: "Limits"
description: "This Page describes the limitations of the Orders API and suggested uses"
---

The Orders API upsert function allows for uploads of order batches.  While the API has a hard limit of 1000 orders per call, we recommend using the order size per call that best suits your needs. There are no concurrency or daily/hourly limits otherwise.
